<div class="mb-4">
    <h2 class="fw-bold text-primary">🗞️ Tous les Gossips</h2>
    <p class="text-muted">Voici la liste de tous les gossips récents :</p>
    <%= link_to "➕ Créer un nouveau gossip", new_gossip_path, class: "btn btn-outline-primary mb-3" %>
</div>

<div id="gossips" class="row g-4">
    <% @gossips.each do |gossip| %>
    <div class="col-sm-12 col-md-6 col-lg-4">
        <div class="card shadow-sm h-100">
            <div class="card-body d-flex flex-column">
                <h5 class="card-title text-dark"><%= gossip.title %></h5>
                <p class="card-text text-secondary">
                    <%= truncate(gossip.content, length: 100, separator: ' ') %>
                </p>
                <div class="mt-auto">
                    <%= link_to "📖 Lire plus", gossip_path(gossip), class: "btn btn-sm btn-primary" %>
                </div>
            </div>
            <div class="card-footer text-muted small">
                Posté le <%= l(gossip.created_at, format: :short) %>
            </div>
        </div>
    </div>
    <% end %>
</div>